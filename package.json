{
  "name": "milk-lab-creations",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "turbo run build",
    "check": "turbo run check",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "dev": "AWS_PROFILE=milklab-dev sst dev",
    "deploy": "sst deploy"
  },
  "devDependencies": {
    "prettier": "^3.6.0",
    "prettier-plugin-svelte": "^4.1.1",
    "sst": "^4.17.1",
    "turbo": "^2.5.0"
  }
}
